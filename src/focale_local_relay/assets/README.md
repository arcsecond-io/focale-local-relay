Place the app icon files for Focale Relay in this directory.

Recommended filenames:
- `app-icon.png` for the in-window logo shown beside the title
- `app-icon.ico` for the Windows packaged application and installer icon

If both files are present, the GUI will use the PNG first and the Windows build
will use the ICO first.
